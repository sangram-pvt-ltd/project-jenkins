apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-app
  namespace: test
  labels:
    app: spring-boot-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-boot-app
  template:
    metadata:
      labels:
        app: spring-boot-app
    spec:
      containers:
      - name: spring-boot-app
        image: 433796583082.dkr.ecr.ap-south-1.amazonaws.com/jenkinsproject:react-92
        ports:
        - containerPort: 80
